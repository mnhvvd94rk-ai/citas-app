{
  "name": "citas-app",
  "version": "0.1.0",
  "private": true,
  "description": "PWA de gestión de citas médicas (monorepo: client + server)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build:client": "npm run build --workspace client"
  }
}
